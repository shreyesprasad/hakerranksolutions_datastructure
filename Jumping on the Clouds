#include <bits/stdc++.h>

using namespace std;


int main()
{
    int n,a[1000],i,m=-1;
    cin>>n;
    for(i=0;i<n;i++)
    cin>>a[i];
    for(i=0;i<n;i++,m++)
    {
        if(i<n-2 && a[i+2]==0)
        i++;
    }
    cout<<m;

    return 0;
}

